[model]
name = Page
label = {{ this.title }}

[fields.menu_title]
label = Menu title
type = string

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[fields.sort_key]
label = Sort order
type = sort_key

[fields.menu_color]
label = Menu color
type = select
choices = yellow, green, red, blue
choice_labels = Yellow, Green, Red, Blue

[children]
order_by = sort_key
